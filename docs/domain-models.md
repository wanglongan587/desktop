# Domain Models

`ora-domain` owns Ora's schema-backed entities and the invariants that must hold regardless of transport or storage. It has no dependency on SQLite, HTTP, Tauri, or `ora-contracts`.

## Entities

Every table owned by the migration catalog in `ora-db` has a first-class domain model:

| Domain type | Backing table |
| --- | --- |
| `Project` | `projects` |
| `Task` | `tasks` |
| `Worktree` | `worktrees` |
| `Session` | `sessions` |
| `Skill` | `skills` |
| `AgentDefinition` | `agents` |
| `ProjectWorkContext` | `project_work_contexts` |
| `VirtualFolder` | `virtual_folders` |
| `VirtualEntry` | `virtual_entries` |
| `Artifact` | `artifacts` |

`VirtualFolder`, `VirtualEntry`, and `Artifact` are declared by the schema and modeled in the domain layer, but no application handler or repository exposes them yet.

Spec documents are the one modeled concept with no backing table. `SpecDocument`, `SpecSource`, `SpecPath`, `SpecContentHash`, and `SpecIdentity` describe files that live in the repository, so the catalog is rebuilt from disk rather than persisted: storing it could only ever disagree with the working tree. `SpecIdentity` distinguishes an identifier the document declared in its frontmatter from one derived from its path, and `SpecContentHash` fingerprints the body so freshness never depends on a modification timestamp. `SpecSource` is a named discovery glob; overlapping matches resolve to the first source in configuration order.

Depending on `ora-domain` is enough to construct and inspect these models; no transport or persistence module needs to be imported.

## Identifiers

Identifiers are newtypes generated by one macro, so a `TaskId` cannot be passed where a `ProjectId` is expected: `ProjectId`, `ProjectWorkContextId`, `TaskId`, `WorktreeId`, `SessionId`, `SkillId`, `AgentDefinitionId`, `VirtualFolderId`, `VirtualEntryId`, `ArtifactId`. Each wraps a `String` and serializes as that bare string, so existing JSON and SQLite text boundaries are unchanged.

## Categorical fields

Numeric and text category columns are modeled as enums rather than raw codes, so callers match exhaustively instead of comparing integers:

- `TaskStatus`: `Todo` (0), `Doing` (1), `Done` (2)
- `SessionStatus`: `Running` (0), `Stopped` (1)
- `WorktreeActivity`: `Inactive` (0), `Active` (1)
- `VirtualEntryKind`: `File` (0), `Directory` (1)
- `AgentCli`: `OpenCode`, `Nga`, `CodeAgentCli`
- `ProjectWorkContextSurface`: `Web`, `Tauri`

Encoding and decoding live at the boundary, not inside the models. Each enum exposes `database_value()` plus a `from_database_value()` that rejects unknown persisted values with a `DomainModelError` instead of constructing an invalid state. Domain enums are never derived from raw integers implicitly.

`AgentCli` is persisted as namespaced text (`ora-space.opencode`, `ora-space.nga`, `ora-space.codeagentcli`) so the stored value does not depend on enum declaration order. It also exposes `executable_name()` for the process lookup performed by the backend agent runtime. `AgentCli::ALL` gives the runtime a stable iteration order over supported CLIs.

`ProjectWorkContextSurface` is persisted as `web` or `tauri`.

## Optionality and audit metadata

Optional fields exist only where the schema column is nullable — for example `Task.worktree_id` and `Worktree.branch_name`. Non-nullable columns stay required in both struct fields and constructors.

`AuditFields` carries `created_at`, `updated_at` (Unix milliseconds), and `is_deleted` for every soft-deletable entity. `ProjectWorkContext` deliberately does not use `AuditFields`: it is lease-based, carries its own `created_at`/`updated_at`, and has no soft-delete column because expired rows are deleted outright rather than flagged.

## Validation in the domain layer

Most models are plain snapshots whose constructors cannot fail. Two normalize and validate instead:

- `Skill::new` trims the name and rejects a blank one with `DomainModelError::EmptySkillName`.
- `AgentDefinition::new` trims the name and rejects a blank one with `DomainModelError::EmptyAgentDefinitionName`.

`Session` models a provider-backed conversation whose routing is immutable: `task_id`, `agent_cli`, and `agent_session_id` are fixed at construction, and `with_status` changes only `status` and `updated_at`. A `Session` cannot be constructed before the provider returns its session identifier, because `agent_session_id` is required rather than optional.

## Boundaries

`ora-domain` does not choose SQL, map contract DTOs, decide transport status codes, or emit logs. Contract shapes belong to `ora-contracts`, use-case rules to `ora-application`, and column mapping to `ora-db`.

See [Application and Contracts Boundary](application-contracts.md) and [Database Migrations](database-migrations.md).
