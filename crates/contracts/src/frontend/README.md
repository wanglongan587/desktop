# Frontend endpoint catalog modules

This directory holds focused endpoint metadata that is aggregated by the root frontend catalog.
Specification endpoints live here to keep the already-large core catalog stable. Each entry remains
transport-neutral and drives the generated TypeScript client manifest used by Web and Tauri.
